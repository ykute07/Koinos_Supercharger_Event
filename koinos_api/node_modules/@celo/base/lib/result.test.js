"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _1 = require(".");
// These tests are just to check the typescript compiler
describe('discriminated collection functions', function () {
    var trueValue = (0, _1.Ok)(true);
    var falseValue = (0, _1.Err)(new Error());
    var collection = [trueValue, falseValue];
    it('can filter to the true value', function () {
        var ok = collection.filter(_1.isOk);
        expect(ok.map(function (_) { return _.result; })).toEqual([true]);
    });
    it('can find in collection', function () {
        var ok = collection.find(_1.isOk);
        if (ok) {
            expect(ok.result).toEqual(true);
        }
    });
});
//# sourceMappingURL=result.test.js.map